#include <iostream>
#include <fstream>
#include <cstdlib>
using namespace std;

const char* Open(ifstream& file, const char* filename){
  file.open(filename);
  return file.is_open() ? NULL : "cannot open file!!";
}

