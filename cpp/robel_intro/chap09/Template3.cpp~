#include <iostream>
#include <string>
using namespace std;

template <typename TYPE>
void FillArray(TYPE* array, size_t size, TYPE value) {
  for (size_t i=0; i<size; i++){
    array[i] = value;
  }
}
