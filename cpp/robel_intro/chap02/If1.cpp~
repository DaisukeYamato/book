#include <iostream>
using namespace std;

void BirthMonth() {
  int month;

  cout << "What is your birth month?" << flush;
  cin >> month;

  if (1 <= monthy && month <= 12) {
    cout << "Your birth month is " << month << "." 
	 << endl;
  }
}
