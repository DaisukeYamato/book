#include <iostream>
using namespace std;

int main() {
  for ( ; ; ) {
    int a, b;
    cout << "Input first value > " << flush;
    cin >> a;

    cout << "Input second value > " << flush;
    cin >> b;

    if (b == 0) {
      cout << "cannot divide by 0!" << endl;
      break;
    }

    cout << a << " / " << b << " = "
	 << a/b << " ... " << a % b << endl;
  }

  cout << "program exitting...";
  
}
